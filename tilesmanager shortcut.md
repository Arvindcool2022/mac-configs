# 🧠 Yabai + skhd Cheat Sheet (macOS)

## 🔹 Modifiers used

- **alt** = ⌥ Option
- **shift** = ⇧ Shift
- **ctrl** = ⌃ Control

---

## 🧭 Window Focus (within a space)

Move focus between tiled windows (Vim-style):

```
alt + h   → focus left
alt + l   → focus right
alt + k   → focus up
alt + j   → focus down
```

---

## 🖥️ Display / Monitor Focus

Move focus between monitors:

```
alt + s   → focus display left
alt + g   → focus display right
```

---

## 🔄 Layout Manipulation

Change how windows are arranged:

```
shift + alt + r   → rotate layout clockwise
shift + alt + y   → mirror layout on Y-axis
shift + alt + x   → mirror layout on X-axis
```

---

## 🪟 Window Tiling / Floating

Control individual windows:

```
shift + alt + t   → toggle float (tile ↔ float)
shift + alt + m   → toggle fullscreen / maximize
shift + alt + e   → balance window sizes
```

---

## 🔁 Swap Windows (reorder without resizing)

Swap current window with neighbors:

```
shift + alt + h   → swap left
shift + alt + l   → swap right
shift + alt + k   → swap up
shift + alt + j   → swap down
```

---

## 🚚 Move Window + Create Split

Move window and split layout:

```
ctrl + alt + h   → move & split left
ctrl + alt + l   → move & split right
ctrl + alt + k   → move & split up
ctrl + alt + j   → move & split down
```

---

## 🖥️ Move Window Between Displays

Send window to another monitor:

```
shift + alt + s   → move window to left display
shift + alt + g   → move window to right display
```

---

## 🧳 Move Window Between Spaces

Send windows across macOS spaces:

```
shift + alt + n   → move window to next space
shift + alt + p   → move window to previous space
```

Move window to a **specific space**:

```
shift + alt + 1   → send window to space 1
shift + alt + 2   → send window to space 2
shift + alt + 3   → send window to space 3
...
shift + alt + 9   → send window to space 9
```

👉 Use macOS default to follow:

```
ctrl + 1 / 2 / 3 ... → jump to space
```

---

## 🧠 Yabai Service Control

Start / stop yabai quickly:

```
ctrl + alt + q   → stop yabai
ctrl + alt + s   → start yabai
ctrl + alt + r   → restart yabai
```

---

## 🖱️ Mouse Shortcuts (with yabai)

While holding **alt**:

```
alt + left-click + drag   → move window
alt + right-click + drag  → resize window
```

Drop window in center → **swap windows**

---

## 🧑‍💻 Terminal / App Shortcuts (macOS & iTerm2)

Useful extras:

```
Cmd + N          → new terminal window
Cmd + T          → new terminal tab
Cmd + D          → split pane vertically
Cmd + Shift + D  → split pane horizontally
Cmd + W          → close window/tab
```

---

## 💡 Pro Tip (Recommended)

Map **Caps Lock → Hyper key**
(Hyper = Cmd + Ctrl + Shift + Alt)

Tools:

- Karabiner-Elements

This avoids shortcut conflicts and makes yabai/skhd even better.

---

## 📌 If you want next

I can:

- turn this into a **printable PDF**
- adapt shortcuts for **external monitor layouts**
- optimize bindings for **one-hand use**
- help you switch to **Hyper-only shortcuts**

Just tell me 👌
